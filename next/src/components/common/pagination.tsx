import { PaginationTypes } from "@/types/strapi";

const Pagination = ({ pagination }: { pagination: PaginationTypes }) => {
  console.log(pagination);
  return <div></div>;
};

export default Pagination;
